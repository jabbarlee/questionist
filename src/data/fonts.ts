import {
    Inter,
    EB_Garamond
} from "next/font/google";

export const merriweather = EB_Garamond({
    subsets: ["latin"],
    weight: ["400", "700"],
    display: "swap",
});

export const inter = Inter({
    subsets: ["latin"],
});